export async function generateCertificatePDF(element, options = {}) {
    console.warn('PDF Generation is temporarily disabled.')
    alert('عذراً، خاصية إنشاء PDF معطلة مؤقتاً للتحديث.')
    return null
}

export async function captureElementAsImage(element, options = {}) {
    console.warn('Image capture is temporarily disabled.')
    return ''
}

export async function generateMultiPagePDF(elements, options = {}) {
    console.warn('Multi-page PDF Generation is temporarily disabled.')
    return null
}
