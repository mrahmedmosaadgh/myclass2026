#!/usr/bin/expect -f

set timeout 300
set password "me123456@A1"

# Run the update script
spawn ./update.sh

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    "(yes/no)?" {
        send "yes\r"
        exp_continue
    }
    eof
}

wait
